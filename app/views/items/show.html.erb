<div class="pt-3">
  <div class="d-flex justify-content-center">
    <div class="d-flex flex-column name-list" style="width: 300px; height: 100vh">
      <div class="px-3 py-3 list-card">
        <div>
          <h2><%= @item.name.capitalize %></h2>
        </div>
        <div>
          <div class="mb-2">
            <% if @item.description.empty? %>
              <%= 'No description available' %>
            <% else %>
              <%= @item.description %>
            <% end %>
          </div>
          <div class="mb-2">
            <%= render "checkbox", item: @item %><%= "Mark as completed" %><br>
            <a href = "<%= edit_item_path(@item) %>"> <i class="fas fa-pencil-alt"></i></a><%= "Edit" %><br>
            <%= link_to item_path(@item), method: :delete,
                  data: {confirm: "Are you sure?"} do %>
                  <i class="far fa-trash-alt"></i>
                  <% end %><%= "Delete" %><br>
          </div>
        </div>
        <div class="mb-2" style="width: 100%; height: 300px"
          data-controller="mapbox"
          data-mapbox-markers-value="<%= @markers.to_json %>"
          data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
        <div>
          <%= link_to "Back", :back %>
        </div>
      </div>
    </div>
  </div>
</div>
