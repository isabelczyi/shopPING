<% @lists.each do |list| %>
  <div style="align-text: center" class="my-3">
    <%= list.name %>
    <%= link_to "Edit", edit_list_path(list) %>
    <%= link_to "Delete", list_path(list),
        method: :delete,
        data: {confirm: "Are you sure?"} %>
  </div>
<% end %>
