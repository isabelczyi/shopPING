<%= simple_form_for [@list, @item] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <h3>locations</h3>
  <div id='locations'>
    <%= f.simple_fields_for :locations do |location| %>
      <%= render 'items/location_fields', :f => location %>
    <% end %>
  </div>
  <%= f.submit 'Save' %>
<% end %>
